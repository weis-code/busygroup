'use client';

export default function ReminderStripePage() {
  return (
    <div style={{ padding: '28px 32px' }}>
      <h1 style={{ fontSize: 20, fontWeight: 700, color: 'var(--t1)', marginBottom: 4 }}>Reminder Stripe</h1>
      <p style={{ fontSize: 12, color: 'var(--t3)', marginBottom: 24 }}>Betalinger og abonnementer</p>
      <div style={{ background: 'var(--s1)', border: '1px solid var(--bd)', borderRadius: 12, padding: '60px 40px', textAlign: 'center', color: 'var(--t3)', fontSize: 13 }}>
        Stripe-integration under opbygning
      </div>
    </div>
  );
}
